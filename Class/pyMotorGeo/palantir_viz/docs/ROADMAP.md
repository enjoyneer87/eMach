# Roadmap (Palantir-Style Flow Viz)

## Near Term

- Improve sequence layout to reduce top-label overlap with very long node names
- Add edge grouping by phase and subsystem tags
- Add export button for current view as PNG/HTML snapshot

## Mid Term

- Add node-level mini timelines (incoming/outgoing events)
- Add filtering by module family (`analysis_*`, `topology_*`, `region_*`)
- Add side panel KPIs (critical path length, fan-in/fan-out)

## Long Term

- Persist user layout presets (JSON)
- Add multi-file PUML merge and cross-diagram traceability
- Move to production server stack for team sharing
