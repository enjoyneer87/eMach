"""Magnetic results helpers (waveforms, effimap, exports).

This module is the preferred name going forward.

For backward compatibility, the original module name `melec_req_check` remains and
re-exports the same functions.
"""

from .melec_req_check import *  # noqa: F401,F403
