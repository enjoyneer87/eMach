# 열해석 핸드오프 — 2026-09-07 (PLAN_20260906 착수: 킷 설치 + D6 완료)

> 전신: [`HANDOFF_20260722.md`](HANDOFF_20260722.md) (FreeFlow/e10 + Fluent 스프레이).
> 이 문서는 계획서 §3 규약("완료 시 HANDOFF에 덧붙인다")에 따른 **moa 측 완료 보고**다.
> 작성 경위: 학위논문 측 세션이 `ssh iso` / `W:` 로 moa 에 접속해 D6 을 대신 실행하고 남겼다.
> `plans/` 는 건드리지 않았다.

---

## 0. 한 줄 요약

`PLAN_20260906_assets` 실행 킷을 moa 에 설치했고 **D6(C67 회전자 원심 응력)을 완료**했다.
D1·D2·D3 는 미착수이나 **선행 블로커가 전부 해소**되어 바로 돌릴 수 있다.

## 1. D6 완료 — C67 회전자 원심 해석

**파일**: `mlxperPJT/thermal/thesis_out/c67_stress.json` (16,496 B) +
`c67_stress_vonmises.png` (114,090 B) / `c67_stress_ur.png` (91,711 B) /
`c67_stress_vs_speed.png` (168,225 B). 전부 300 KB 이하, `git check-ignore` 통과 확인.

**핵심 수치 3줄** (SYS-5, 정적 원심, 2D 평면, 축 Z, 40,922 요소 / 124,105 절점):
- 18,000 rpm 최대 von Mises **499.03 MPa** @ node 245 (r 62.688 mm, θ 22.07°)
- 최대 반경변위 **0.031364 mm** @ node 107725 → 공극 0.80 mm 의 **3.92 %**
- 속도 스윕 12/15/18/21 krpm = **221.79 / 346.55 / 499.03 / 679.24 MPa** (ω² 스케일링)

회전원환 해석해 게이트 PASS(18 krpm 내경 후프 131.96 MPa 대비 3.8배, 정상범위 2~4배).
항복강도·안전계수는 계획서 69행대로 `null` — 20PN1150F 데이터시트 값은 논문 쪽에서 채운다.
재료는 `ds.dat` 실측: E 192,500 MPa · ρ 7.65e-9 tonne/mm³ · ν 0.3 (계획서 값과 일치).

### 🔴 계획서 66행의 rst 경로는 틀렸다 — 실제 출처

`E:\KDH\251114_C67_test\C67_stress_files\dp0\SYS-5\MECH\file.rst` 는 **moa 에도 학위논문 PC 에도 없다.**
실제로 찾은 곳은 **NAS 아카이브** 안이다:

```
N:\프로젝트\현대차_항공사업_파트너제안\Motor_MCAD_Stress\C67_stress.wbpz   (127,483,259 B)
  └ C67_stress_files/dp0/SYS-5/MECH/file.rst   26,607,616 B
```

`.wbpz` 는 ZIP 이라 압축을 풀지 않고 목록만 읽어 확인했고, 필요한 9개 멤버만 뽑아
`C:\work\_thermal_kit_20260907\c67\C67_stress_extract\` 에 두었다(110 MB, SYS-3/4/5 × rst·ds.dat·solve.out).

| 시스템 | 정체 | 규모 |
|---|---|---|
| SYS-1 | **LS-DYNA 외연**(d3plot·binout·ncforc). `.rst` 없음 | — |
| SYS-2 | `ds.dat` 만 있고 `file.rst` 없음(미해석 추정) | 166,420 절점 |
| SYS-3 (F5) | 정적 원심, 조밀도 낮음 | 73,123 절점 |
| SYS-4 (G5) | **BISO 소성**(재료 비선형) | 73,123 절점 |
| SYS-5 (H5) | 정적 원심, 최다 절점 — **D6 이 쓴 것** | 124,105 절점 |

★ **SYS-4 가 소성 FE 라는 게 중요하다.** `tools/motorCAD/pyMCAD/stress.py` 의 Neuber/Glinka
**해석적** 소성보정과 **직접 탄소성 FE** 를 같은 모델에서 비교할 수 있다(3장 소재).

## 2. 킷 설치 상태

`mlxperPJT/thermal/plans/PLAN_20260906_assets/` 11개 파일. **읽기전용 레퍼런스**이므로
`freeflow/scripts/20_~24_` 로 복사·승격해 쓰고 원본은 고치지 않는다. 상세는 그 폴더의 `README.md`.

moa 는 GitHub 에 도달하지 못하므로(`Could not resolve host: github.com`) 코드는 전부
**git bundle** 로 들어왔다. 되돌려 보낼 때도 같은 방식이어야 한다.

적용된 커밋: `f7f2ad6`(계획서) → `5a4dc19`(D6 추가) → `556f0a1`(킷) → `e5d6b86`(D6 수정 2건).

### D6 스크립트 수정 2건 (실데이터에 물려서야 드러남)

1. **2D 평면 결과 미지원** — `nodal_solution()` 이 `(n,2)` 를 주는데 `(n,>=3)` 을 요구해 FATAL.
   Motor-CAD 기계해석은 2D(UX·UY)다. 축이 Z 이므로 `u_r = ux·cosθ + uy·sinθ` 는 그대로 성립.
2. **재솔브 이중 OMEGA** — `ds.dat` 에 이미 회전속도가 있는데 `OMEGA` 를 덧씌워 4배가 됐다
   (같은 18 krpm 에서 rst 499.03 vs 재솔브 1996.14 MPa). 기본 스윕을 `scale` 로 바꾸고,
   `resolve` 시 기준 rst 와 교차검증해 비율이 4.0 근처면 경고하도록 했다.

## 3. 환경 확인 (preflight.py 실측, FAIL 0 · exit 0)

- venv `PyMotorEnv_310`: numpy 2.2.6 · matplotlib 3.10.8 · pyvista 0.47.0 ·
  ansys.mapdl.core 0.73.2 · ansys.mapdl.reader 0.56.0 · ansys.motorcad.core 0.8.4
- `ansys.dpf.core` **없음** — 정상이다. 킷은 DPF 를 쓰지 않는다.
- 🟢 **`ff_e10_mesh_v2.cdb` 있음** — `D:\KDH\simVary\Ansys_Thermal\`, 248.2 MB, 2026-07-22.
  HANDOFF §11-4 가 경고한 1순위 블로커가 해소된 상태다.
- Malgun Gothic 있음, `AWP_ROOT` 정상, D: 여유 340 GB
- ⚠️ matplotlib 3.10.8 이라 `thermal_viz.py:456` 의 `matplotlib.cm.get_cmap` 이 제거됐다.
  **회로 오버레이 경로만** 죽는다(`circuit_3d_png`/`circuit_3d_gif`/`full_dashboard_gif`).
  `contour_png`/`cut3d_png`/`component_png` 는 안전. 고치려면 `matplotlib.colormaps[CMAP]` 로.

### `.gitignore` 수정함

루트 `.gitignore:39 *.png` 가 `thesis_out/*.png` 를 삼키고 있었다. 말미에 negation 을 붙였다.
`plans/` 밖이라 학위논문 세션이 못 고치는 항목이었고, 이번에 moa 측에서 처리했다.

```
!mlxperPJT/thermal/thesis_out/*.png
!mlxperPJT/thermal/thesis_out/*.csv
```

## 4. 남은 작업

### D1 (최우선) — 연속 정격 곡선
메시·손실맵·환경 전부 준비됐다. **계획서의 64런 브루트포스 대신 중첩(superposition)** 이 기본이다.
모델이 완전 선형(상수 k·h·G, 복사 없음, Dirichlet 은 OIL 70 °C 하나)이라
`T = 70 + Σ(P_s·G_s)` 가 정확하다. h세트당 단위솔브 5회 = 10솔브로 64조합을 복원하고
직접솔브 2회로 0.05 K 이내를 검증한다. 계획서 문자 그대로가 필요하면 `--mode brute`.

```
"C:\Users\moa\.ansys_python_venvs\PyMotorEnv_310\Scripts\python.exe" ^
  mlxperPJT\thermal\plans\PLAN_20260906_assets\d1_cont_rating.py ^
  --repo D:\KDH\NvidiaNemo\eMach --cdb D:\KDH\simVary\Ansys_Thermal\ff_e10_mesh_v2 ^
  --log D:\KDH\d1.log
```

⚠️ 계획서의 "13.5 s/런 × 64 = 15분" 은 v1 메시(544k·회로없음) 기준이라 틀리다.
v2 는 1.11M 절점이라 런당 40~60 s 다.
⚠️ 16 krpm 에서는 최저 격자전류 115 A 에서도 180 °C 를 넘을 것으로 예측된다.
계획서식 4점 보간이 외삽이 되므로 중첩 + 연속 근찾기가 필요한 실질적 이유다.

### D2 — 턴별 AC 주입
메시에 턴 아이덴티티가 없다. mat 3 을 스택 z구간 안에서 **반경 6밴드로 실측 분할**한다
(하드코딩 금지). 밴드별 요소수·체적·반경범위 sanity 블록을 먼저 눈으로 볼 것.

### D3 — 중력 수정 후 FreeFlow 8 s 재솔브
🟢 **기존 결과가 12.1 GB 남아 있다**(`.sph` 2,406개, `Project_thermal` 프레임 0~800).
5시간짜리 재솔브 전에 `d3_htc_backout.py --sph` 를 기존 결과에 먼저 돌려 파이프라인을
검증할 수 있다. 중력이 아직 축방향이라 그 수치를 논문에 쓸 수는 없다.

⚠️ HANDOFF_20260722 §10 과 어긋나는 관측 2건 — 그 문서가 07-22 에 쓰인 뒤 벌어진 일로 보인다.
- §10-1 은 8초 솔브가 t=6.53 s 에서 중단됐다고 했으나, `Project_thermal` 의 프레임이
  완주한 유동전용 런과 같은 **801개(0~800)** 이고 기록 시각이 **2026-07-23 22:58** 이다.
- §10-2 는 2-way System Coupling 을 "미착수" 로 적었으나 `Project_2way.freeflow` 가
  **2026-07-23 23:12** 에 있다(결과셋 5.28 GB).
실제 도달 시간은 결과 내용을 열어야 확정된다. 이번 세션은 메타데이터만 봤다.

### 그 외 관측

- JAC279 하이브리드 런(권선 152.2 °C)의 `file.rth` 는 **사라졌다.** scratchpad 런 디렉터리
  5개가 전부 0 파일이다. 수치 자체는 `freeflow/data/ff_mapdl_hybrid_temps.json` 에 남아 있다.
- e10 구조해석 결과도 있다: `D:\KDH\simVary\e10_20251226\e10_User_wB_files\dp0\SYS\MECH\file.rst`
  (19.1 MB). NAS 아카이브 `e10_User_wB.wbpz` 에는 `.rst` 가 셋(74.9 / 42.0 / 40.7 MB).
